[package]
name = "moonsnap-media"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
ffmpeg-sidecar = "2.4"
image = { version = "0.25", default-features = false, features = ["gif", "jpeg", "png", "webp"] }
log = "0.4"
serde_json = "1"
