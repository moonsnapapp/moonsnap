{
  "name": "snapit",
  "private": true,
  "version": "0.4.22",
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:desktop": "turbo dev --filter=@snapit/desktop",
    "dev:web": "cd apps/web && bun run dev",
    "dev:all": "concurrently -k -n desktop,web -c blue,magenta \"bun run tauri:dev\" \"bun run dev:web\"",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "test:run": "turbo test:run",
    "typecheck": "turbo typecheck",
    "tauri": "cd apps/desktop && tauri",
    "tauri:dev": "cd apps/desktop && tauri dev",
    "tauri:build": "cd apps/desktop && tauri build",
    "version": "node scripts/sync-versions.js && git add -A",
    "postversion": "git push && git push --tags",
    "release": "npm version patch",
    "release:minor": "npm version minor",
    "release:major": "npm version major",
    "prepare": "husky"
  },
  "devDependencies": {
    "concurrently": "9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "turbo": "2.8.0"
  },
  "lint-staged": {
    "apps/desktop/**/*.{ts,tsx}": [
      "eslint --fix --config apps/desktop/eslint.config.js"
    ],
    "apps/web/**/*.{ts,tsx}": [
      "eslint --fix --config apps/web/eslint.config.mjs"
    ],
    "packages/ui/**/*.{ts,tsx}": [
      "eslint --fix --config packages/ui/eslint.config.js"
    ],
    "apps/desktop/src-tauri/**/*.rs": [
      "cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml --"
    ]
  }
}
